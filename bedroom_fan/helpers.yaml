# Create these via Settings -> Devices & Services -> Helpers in the UI, or
# paste into configuration.yaml if you run helpers in YAML mode.

input_boolean:
  bedroom_fan_night_phase:
    name: Bedroom Fan Night Phase
  bedroom_fan_manual_override:
    name: Bedroom Fan Manual Override
    icon: mdi:hand-back-right

input_datetime:
  bedroom_fan_last_command_time:
    name: Bedroom Fan Last Command Time
    has_date: true
    has_time: true
